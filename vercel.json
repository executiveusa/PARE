{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf out apps/web/out && env -u OD_WEB_DIST_DIR -u OD_WEB_PROD NODE_ENV=production OD_WEB_OUTPUT_MODE= pnpm --filter @open-design/web build && test -f apps/web/out/index.html && cp -R apps/web/out out",
  "installCommand": "pnpm install",
  "outputDirectory": "out",
  "framework": null
}
