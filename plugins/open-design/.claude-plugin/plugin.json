{
  "name": "open-design",
  "displayName": "PARÉ",
  "version": "1.0.0",
  "description": "PARÉ sovereign studio compatibility plugin · projects, live artifacts and approved connector tools exposed to Claude Code over MCP. Requires the `od` compatibility CLI and a reachable PARÉ daemon.",
  "mcpServers": "./.mcp.json"
}
